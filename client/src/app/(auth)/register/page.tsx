import RegisterForm from "@/components/RegisterFormCard";

export default function RegisterPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-muted">
      <RegisterForm />
    </main>
  );
}
